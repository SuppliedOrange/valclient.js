/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type PhantomChromaType<T> =   T extends "Task Force 809 Phantom"
    ? "Default"
    :
    T extends "RGX 11z Pro Phantom"
    ? "Default" | "Red" | "Blue" | "Yellow"
    :
    T extends "Xenohunter Phantom"
    ? "Default"
    :
    T extends "VALORANT GO! Vol. 1 Phantom"
    ? "Default"
    :
    T extends "Araxys Phantom"
    ? "Default" | "Purple" | "Black" | "Silver"
    :
    T extends "Neptune Phantom"
    ? "Default" | "Black"
    :
    T extends "Radiant Entertainment System Phantom"
    ? "Default" | "DanceFever" | "K.nockO.ut!!"
    :
    T extends "Gaia's Vengeance Phantom"
    ? "Default" | "Blue" | "Green" | "Orange"
    :
    T extends "Spectrum Phantom"
    ? "Default" | "Black" | "Red" | "Purple/Pink"
    :
    T extends "Piedra del Sol Phantom"
    ? "Default"
    :
    T extends "Composite Phantom"
    ? "Default"
    :
    T extends "Bolt Phantom"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "Aperture Phantom"
    ? "Default"
    :
    T extends "Overlay Phantom"
    ? "Default" | "Black" | "Pink" | "Grey"
    :
    T extends "Champions 2022 Phantom"
    ? "Default"
    :
    T extends "Champions 2024 Phantom"
    ? "Default"
    :
    T extends "Tactiplay Phantom"
    ? "Default"
    :
    T extends "Radiant Crisis 001 Phantom"
    ? "Default"
    :
    T extends "Nebula Phantom"
    ? "Default"
    :
    T extends "Nocturnum Phantom"
    ? "Default" | "Black" | "Blue" | "Yellow"
    :
    T extends "Doom Wing Phantom"
    ? "Default"
    :
    T extends "Undercity Phantom"
    ? "Default"
    :
    T extends "Doombringer Phantom"
    ? "Default" | "Red" | "Blue" | "Green"
    :
    T extends "Glitchpop Phantom"
    ? "Default" | "Blue" | "Red" | "Gold"
    :
    T extends "ChronoVoid Phantom"
    ? "Default" | "Purple" | "Red" | "Black"
    :
    T extends "Emberclad Phantom"
    ? "Default"
    :
    T extends "Abyssal Phantom"
    ? "Default"
    :
    T extends "Doodle Buds Phantom"
    ? "Default" | "Tactifriends" | "LeagueofLegends"
    :
    T extends "Singularity Phantom"
    ? "Default" | "Blue" | "Red" | "Purple"
    :
    T extends "Sarmad Phantom"
    ? "Default"
    :
    T extends "Rush Phantom"
    ? "Default"
    :
    T extends "Chromedek Phantom"
    ? "Default"
    :
    T extends "Velocity Phantom"
    ? "Default" | "Yellow" | "Purple" | "Aquamarine"
    :
    T extends "Team Ace Phantom"
    ? "Default"
    :
    T extends "Topotek Phantom"
    ? "Default"
    :
    T extends "Nanobreak Phantom"
    ? "Default"
    :
    T extends "Neo Frontier Phantom"
    ? "Default" | "Gold" | "Silver" | "Copper"
    :
    T extends "Soulstrife Phantom"
    ? "Default"
    :
    T extends "Primordium Phantom"
    ? "Default" | "Venom" | "Cobalt" | "Gold"
    :
    T extends "Hieroscape Phantom"
    ? "Default"
    :
    T extends "Prime//2.0 Phantom"
    ? "Default" | "Gold" | "Green" | "Orange"
    :
    T extends "Avalanche Phantom"
    ? "Default"
    :
    T extends "Bound Phantom"
    ? "Default"
    :
    T extends "Prism Phantom"
    ? "Default"
    :
    T extends "Serenity Phantom"
    ? "Default"
    :
    T extends "Ruination Phantom"
    ? "Default" | "Purple" | "Red/Yellow" | "Green"
    :
    T extends "Kingdom Phantom"
    ? "Default"
    :
    T extends "Kohaku & Matsuba Phantom"
    ? "Default" | "Dark"
    :
    T extends "Smite Phantom"
    ? "Default"
    :
    T extends "Daydreams Phantom"
    ? "Default"
    :
    T extends "RDVR Phantom"
    ? "Default"
    :
    T extends "Celestial Phantom"
    ? "Default"
    :
    T extends "Magepunk Phantom"
    ? "Default" | "Green" | "Black/Red" | "Yellow"
    :
    T extends "Spline Phantom"
    ? "Default" | "Green" | "Red" | "Blue"
    :
    T extends "Minima Phantom"
    ? "Default"
    :
    T extends "Combat Crafts Phantom"
    ? "Default"
    :
    T extends "Galleria Phantom"
    ? "Default"
    :
    T extends "Silvanus Phantom"
    ? "Default"
    :
    T extends "Ion Phantom"
    ? "Default"
    :
    T extends "Oni Phantom"
    ? "Default" | "Kumo" | "Hana" | "Tsubame"
    :
    T extends "Switchback Phantom"
    ? "Default"
    :
    T extends "9 Lives Phantom"
    ? "Default" | "Raze" | "Reyna" | "Viper"
    :
    T extends "Hue Shift Phantom"
    ? "Default"
    :
    T extends "Aero Phantom"
    ? "Default" | "Cream/Pink" | "Purple/Gray" | "Green/Black"
    :
    T extends "Artisan Phantom"
    ? "Default" | "Red" | "Green" | "Blue"
    :
    T extends "Protocol 781-A Phantom"
    ? "Default" | "White/Pink" | "Red/Blue" | "Green"
    :
    T extends "Lightwave Phantom"
    ? "Default" | "Blue" | "Red" | "Gray"
    :
    T extends "Orion Phantom"
    ? "Default"
    :
    T extends "Sentinels of Light Phantom"
    ? "Default" | "Pink" | "Red/Green" | "Blue/Purple"
    :
    T extends "Shellspire Phantom"
    ? "Default"
    :
    T extends "Helix Phantom"
    ? "Default" | "Gold" | "Purple" | "Silver"
    :
    T extends "Snowfall Phantom"
    ? "Default"
    :
    T extends "Reaver Phantom"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "Sovereign Phantom"
    ? "Default" | "Green" | "Teal" | "Red"
    :
    T extends "Recon Phantom"
    ? "Default" | "RedCamo" | "BlueCamo" | "GreenCamo"
    :
    T extends "Mystbloom Phantom"
    ? "Default" | "Blue" | "Orange" | "Purple"
    :
    T extends "Infinity Phantom"
    ? "Default" | "White" | "Yellow" | "Pink"
    :
    T extends "Reverie Phantom"
    ? "Default"
    :
    T extends "Standard Phantom"
    ? "Default"
    :
    T extends "Fortune's Hand Phantom"
    ? "Default"
    :
    T extends "Tigris Phantom"
    ? "Default"
    :
    T extends "BlastX Phantom"
    ? "Default" | "Black" | "Yellow" | "Pink"
    :
    T extends "Solarex Phantom"
    ? "Default" | "Yellow" | "Green" | "Purple"
    :
    T extends "Winterwunderland Phantom"
    ? "Default"
    :
    T extends "Convergence Phantom"
    ? "Default"
    :
  unknown;
