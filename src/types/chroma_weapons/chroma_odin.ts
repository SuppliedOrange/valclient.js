/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type OdinChromaType<T> =   T extends "Altitude Odin"
    ? "Default"
    :
    T extends "Xenohunter Odin"
    ? "Default"
    :
    T extends "Neptune Odin"
    ? "Default" | "Black"
    :
    T extends "Tactiplay Odin"
    ? "Default"
    :
    T extends "Rune Stone Odin"
    ? "Default"
    :
    T extends "Comet Odin"
    ? "Default" | "Red" | "Pink" | "Yellow"
    :
    T extends "Doom Wing Odin"
    ? "Default"
    :
    T extends "Doombringer Odin"
    ? "Default" | "Red" | "Blue" | "Green"
    :
    T extends "Glitchpop Odin"
    ? "Default" | "Blue" | "Red" | "Gold"
    :
    T extends "Nitro Odin"
    ? "Default"
    :
    T extends "Topotek Odin"
    ? "Default"
    :
    T extends "Nanobreak Odin"
    ? "Default"
    :
    T extends ".EXE Odin"
    ? "Default"
    :
    T extends "Neo Frontier Odin"
    ? "Default" | "Gold" | "Silver" | "Copper"
    :
    T extends "Prime//2.0 Odin"
    ? "Default" | "Gold" | "Green" | "Orange"
    :
    T extends "Prism III Odin"
    ? "Default" | "Pink" | "Green" | "Purple"
    :
    T extends "Smite Odin"
    ? "Default"
    :
    T extends "Coalition: Cobra Odin"
    ? "Default" | "Blue" | "Red" | "Orange"
    :
    T extends "Lightwave Odin"
    ? "Default" | "Blue" | "Red" | "Gray"
    :
    T extends "Sensation Odin"
    ? "Default"
    :
    T extends "Orion Odin"
    ? "Default"
    :
    T extends "Sentinels of Light Odin"
    ? "Default" | "Pink" | "Red/Green" | "Blue/Purple"
    :
    T extends "Schema Odin"
    ? "Default"
    :
    T extends "Lycan's Bane Odin"
    ? "Default"
    :
    T extends "Reaver Odin"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "Sovereign Odin"
    ? "Default" | "Green" | "Teal" | "Red"
    :
    T extends "Standard Odin"
    ? "Default"
    :
    T extends "Evori Dreamwings Odin"
    ? "Default" | "Blue" | "Green" | "Pink"
    :
    T extends "Freehand Odin"
    ? "Default"
    :
    T extends "Aerosol Odin"
    ? "Default"
    :
    T extends "Fortune's Hand Odin"
    ? "Default"
    :
    T extends "BlastX Odin"
    ? "Default" | "Black" | "Yellow" | "Pink"
    :
    T extends "Solarex Odin"
    ? "Default" | "Yellow" | "Green" | "Purple"
    :
  unknown;
