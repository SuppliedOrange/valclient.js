/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type ClassicChromaType<T> =   T extends "Resolution Classic"
    ? "Default"
    :
    T extends "Steel Resolve Classic"
    ? "Default"
    :
    T extends "Fiber Optic Classic"
    ? "Default" | "Silver" | "Gold" | "Emerald"
    :
    T extends "RGX 11z Pro Classic"
    ? "Default" | "Red" | "Blue" | "Yellow"
    :
    T extends "Intergrade Classic"
    ? "Default"
    :
    T extends "VALORANT GO! Vol. 2 Classic"
    ? "Default"
    :
    T extends "Divergence Classic"
    ? "Default" | "Purple" | "Blue" | "Green"
    :
    T extends "Neptune Classic"
    ? "Default" | "Black"
    :
    T extends "Spectrum Classic"
    ? "Default" | "Black" | "Red" | "Purple/Pink"
    :
    T extends "Pistolinha Classic"
    ? "Default"
    :
    T extends "Striker Classic"
    ? "Default" | "Green/Yellow/Black" | "Pink/Teal/Purple" | "Blue/White/Red"
    :
    T extends "Radiant Crisis 001 Classic"
    ? "Default"
    :
    T extends "Nocturnum Classic"
    ? "Default" | "Black" | "Blue" | "Yellow"
    :
    T extends "Red Alert Classic"
    ? "Default"
    :
    T extends "Undercity Classic"
    ? "Default"
    :
    T extends "Glitchpop Classic"
    ? "Default" | "Blue" | "Red" | "Gold"
    :
    T extends "Finesse Classic"
    ? "Default"
    :
    T extends "Surge Classic"
    ? "Default" | "Black" | "Yellow" | "Blue"
    :
    T extends "Songsteel Classic"
    ? "Default"
    :
    T extends "Forsaken Classic"
    ? "Default" | "Gold"
    :
    T extends "Shimmer Classic"
    ? "Default" | "Gold" | "Red" | "Teal"
    :
    T extends "FIRE/arm Classic"
    ? "Default"
    :
    T extends "Goldwing Classic"
    ? "Default"
    :
    T extends "Prime Classic"
    ? "Default" | "Orange" | "Blue" | "Yellow"
    :
    T extends "Avalanche Classic"
    ? "Default"
    :
    T extends "Bubble Pop Classic"
    ? "Default" | "Red"
    :
    T extends "Black.Market Classic"
    ? "Default"
    :
    T extends "Bound Classic"
    ? "Default"
    :
    T extends "Prism III Classic"
    ? "Default" | "Pink" | "Green" | "Purple"
    :
    T extends "Yoonseul Classic"
    ? "Default"
    :
    T extends "Kingdom Classic"
    ? "Default"
    :
    T extends "Kohaku & Matsuba Classic"
    ? "Default" | "Dark"
    :
    T extends "Smite Classic"
    ? "Default"
    :
    T extends "Daydreams Classic"
    ? "Default"
    :
    T extends "Spline Classic"
    ? "Default" | "Green" | "Red" | "Blue"
    :
    T extends "Tacti-Series Classic"
    ? "Default"
    :
    T extends "Galleria Classic"
    ? "Default"
    :
    T extends "Switchback Classic"
    ? "Default"
    :
    T extends "Cryostasis Classic"
    ? "Default"
    :
    T extends "9 Lives Classic"
    ? "Default" | "Raze" | "Reyna" | "Viper"
    :
    T extends "Premiere Collision Classic"
    ? "Default"
    :
    T extends "Gravitational Uranium Neuroblaster Classic"
    ? "Default" | "Chrome" | "Black" | "Red/Blue"
    :
    T extends "Panoramic Classic"
    ? "Default"
    :
    T extends "Sakura Classic"
    ? "Default"
    :
    T extends "Torque Classic"
    ? "Default" | "Yellow" | "Blue" | "Orange"
    :
    T extends "Snowfall Classic"
    ? "Default"
    :
    T extends "Infinity Classic"
    ? "Default" | "White" | "Yellow" | "Pink"
    :
    T extends "Reverie Classic"
    ? "Default"
    :
    T extends "Standard Classic"
    ? "Default"
    :
    T extends "Refractrix Classic"
    ? "Default"
    :
    T extends "Final Chamber Classic"
    ? "Default"
    :
    T extends "VCT x 100T Classic"
    ? "Default"
    :
    T extends "VCT x AG Classic"
    ? "Default"
    :
    T extends "VCT x BBL Classic"
    ? "Default"
    :
    T extends "VCT x BLD Classic"
    ? "Default"
    :
    T extends "VCT x BLG Classic"
    ? "Default"
    :
    T extends "VCT x C9 Classic"
    ? "Default"
    :
    T extends "VCT x DFM Classic"
    ? "Default"
    :
    T extends "VCT x DRG Classic"
    ? "Default"
    :
    T extends "VCT x DRX Classic"
    ? "Default"
    :
    T extends "VCT x EDG Classic"
    ? "Default"
    :
    T extends "VCT x EG Classic"
    ? "Default"
    :
    T extends "VCT x FNC Classic"
    ? "Default"
    :
    T extends "VCT x FPX Classic"
    ? "Default"
    :
    T extends "VCT x FUR Classic"
    ? "Default"
    :
    T extends "VCT x FUT Classic"
    ? "Default"
    :
    T extends "VCT x G2 Classic"
    ? "Default"
    :
    T extends "VCT x GEN Classic"
    ? "Default"
    :
    T extends "VCT x GE Classic"
    ? "Default"
    :
    T extends "VCT x GX Classic"
    ? "Default"
    :
    T extends "VCT x JDG Classic"
    ? "Default"
    :
    T extends "VCT x KC Classic"
    ? "Default"
    :
    T extends "VCT x KOI Classic"
    ? "Default"
    :
    T extends "VCT x KRÜ Classic"
    ? "Default"
    :
    T extends "VCT x LEV Classic"
    ? "Default"
    :
    T extends "VCT x LOUD Classic"
    ? "Default"
    :
    T extends "VCT x M8 Classic"
    ? "Default"
    :
    T extends "VCT x MIBR Classic"
    ? "Default"
    :
    T extends "VCT x NAVI Classic"
    ? "Default"
    :
    T extends "VCT x NOVA Classic"
    ? "Default"
    :
    T extends "VCT x NRG Classic"
    ? "Default"
    :
    T extends "VCT x PRX Classic"
    ? "Default"
    :
    T extends "VCT x RRQ Classic"
    ? "Default"
    :
    T extends "VCT x SEN Classic"
    ? "Default"
    :
    T extends "VCT x T1 Classic"
    ? "Default"
    :
    T extends "VCT x TE Classic"
    ? "Default"
    :
    T extends "VCT x TEC Classic"
    ? "Default"
    :
    T extends "VCT x TH Classic"
    ? "Default"
    :
    T extends "VCT x TL Classic"
    ? "Default"
    :
    T extends "VCT x TLN Classic"
    ? "Default"
    :
    T extends "VCT x TS Classic"
    ? "Default"
    :
    T extends "VCT x TYL Classic"
    ? "Default"
    :
    T extends "VCT x VIT Classic"
    ? "Default"
    :
    T extends "VCT x WOL Classic"
    ? "Default"
    :
    T extends "VCT x ZETA Classic"
    ? "Default"
    :
    T extends "VCT25 x 100T Classic"
    ? "Default"
    :
    T extends "VCT25 x 2G Classic"
    ? "Default"
    :
    T extends "VCT25 x AG Classic"
    ? "Default"
    :
    T extends "VCT25 x APK Classic"
    ? "Default"
    :
    T extends "VCT25 x BBL Classic"
    ? "Default"
    :
    T extends "VCT25 x BLG Classic"
    ? "Default"
    :
    T extends "VCT25 x BOOM Classic"
    ? "Default"
    :
    T extends "VCT25 x C9 Classic"
    ? "Default"
    :
    T extends "VCT25 x DFM Classic"
    ? "Default"
    :
    T extends "VCT25 x DRG Classic"
    ? "Default"
    :
    T extends "VCT25 x DRX Classic"
    ? "Default"
    :
    T extends "VCT25 x EDG Classic"
    ? "Default"
    :
    T extends "VCT25 x EG Classic"
    ? "Default"
    :
    T extends "VCT25 x FNC Classic"
    ? "Default"
    :
    T extends "VCT25 x FPX Classic"
    ? "Default"
    :
    T extends "VCT25 x FUR Classic"
    ? "Default"
    :
    T extends "VCT25 x FUT Classic"
    ? "Default"
    :
    T extends "VCT25 x G2 Classic"
    ? "Default"
    :
    T extends "VCT25 x GEN Classic"
    ? "Default"
    :
    T extends "VCT25 x GE Classic"
    ? "Default"
    :
    T extends "VCT25 x GX Classic"
    ? "Default"
    :
    T extends "VCT25 x JDG Classic"
    ? "Default"
    :
    T extends "VCT25 x KC Classic"
    ? "Default"
    :
    T extends "VCT25 x KOI Classic"
    ? "Default"
    :
    T extends "VCT25 x KRÜ Classic"
    ? "Default"
    :
    T extends "VCT25 x LEV Classic"
    ? "Default"
    :
    T extends "VCT25 x LOUD Classic"
    ? "Default"
    :
    T extends "VCT25 x M8 Classic"
    ? "Default"
    :
    T extends "VCT25 x MIBR Classic"
    ? "Default"
    :
    T extends "VCT25 x NAVI Classic"
    ? "Default"
    :
    T extends "VCT25 x NOVA Classic"
    ? "Default"
    :
    T extends "VCT25 x NRG Classic"
    ? "Default"
    :
    T extends "VCT25 x NS Classic"
    ? "Default"
    :
    T extends "VCT25 x PRX Classic"
    ? "Default"
    :
    T extends "VCT25 x RRQ Classic"
    ? "Default"
    :
    T extends "VCT25 x SEN Classic"
    ? "Default"
    :
    T extends "VCT25 x T1 Classic"
    ? "Default"
    :
    T extends "VCT25 x TE Classic"
    ? "Default"
    :
    T extends "VCT25 x TEC Classic"
    ? "Default"
    :
    T extends "VCT25 x TH Classic"
    ? "Default"
    :
    T extends "VCT25 x TL Classic"
    ? "Default"
    :
    T extends "VCT25 x TLN Classic"
    ? "Default"
    :
    T extends "VCT25 x TS Classic"
    ? "Default"
    :
    T extends "VCT25 x TYL Classic"
    ? "Default"
    :
    T extends "VCT25 x VIT Classic"
    ? "Default"
    :
    T extends "VCT25 x WOL Classic"
    ? "Default"
    :
    T extends "VCT25 x XLG Classic"
    ? "Default"
    :
    T extends "VCT25 x ZETA Classic"
    ? "Default"
    :
    T extends "Storm Maw Classic"
    ? "Default"
    :
  unknown;
