/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type SpectreChromaType<T> =   T extends "Task Force 809 Spectre"
    ? "Default"
    :
    T extends "Fiber Optic Spectre"
    ? "Default" | "Silver" | "Gold" | "Emerald"
    :
    T extends "RGX 11z Pro Spectre"
    ? "Default" | "Red" | "Blue" | "Yellow"
    :
    T extends "Intergrade Spectre"
    ? "Default"
    :
    T extends "VALORANT GO! Vol. 1 Spectre"
    ? "Default"
    :
    T extends "Neptune Spectre"
    ? "Default" | "Black"
    :
    T extends "Celestia Spectre"
    ? "Default" | "Silver" | "RoseGold" | "Gold"
    :
    T extends "Bolt Spectre"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "Striker Spectre"
    ? "Default" | "Green/Yellow/Black" | "Pink/Teal/Purple" | "Blue/White/Red"
    :
    T extends "Comet Spectre"
    ? "Default" | "Red" | "Pink" | "Yellow"
    :
    T extends "Radiant Crisis 001 Spectre"
    ? "Default"
    :
    T extends "Starlit Odyssey Spectre"
    ? "Default" | "Twilight" | "Sunset" | "Midnight"
    :
    T extends "Emberclad Spectre"
    ? "Default"
    :
    T extends "Abyssal Spectre"
    ? "Default"
    :
    T extends "Singularity Spectre"
    ? "Default" | "Blue" | "Red" | "Purple"
    :
    T extends "Sarmad Spectre"
    ? "Default"
    :
    T extends "Hivemind Spectre"
    ? "Default"
    :
    T extends "Venturi Spectre"
    ? "Default"
    :
    T extends "Forsaken Spectre"
    ? "Default" | "Gold"
    :
    T extends "Velocity Spectre"
    ? "Default" | "Yellow" | "Purple" | "Aquamarine"
    :
    T extends "Soulstrife Spectre"
    ? "Default"
    :
    T extends "Primordium Spectre"
    ? "Default" | "Venom" | "Cobalt" | "Gold"
    :
    T extends "Infantry Spectre"
    ? "Default"
    :
    T extends "EX.O Spectre"
    ? "Default" | "Yellow" | "Red" | "Pink"
    :
    T extends "Horizon Spectre"
    ? "Default"
    :
    T extends "Prime Spectre"
    ? "Default" | "Orange" | "Blue" | "Yellow"
    :
    T extends "Avalanche Spectre"
    ? "Default"
    :
    T extends "Prism Spectre"
    ? "Default"
    :
    T extends "Serenity Spectre"
    ? "Default"
    :
    T extends "Yoonseul Spectre"
    ? "Default"
    :
    T extends "Ruination Spectre"
    ? "Default" | "Purple" | "Red/Yellow" | "Green"
    :
    T extends "Kingdom Spectre"
    ? "Default"
    :
    T extends "Blush Spectre"
    ? "Default"
    :
    T extends "Monstrocity Spectre"
    ? "Default" | "Brown" | "Blue" | "Yellow"
    :
    T extends "Luxe Spectre"
    ? "Default"
    :
    T extends "Magepunk Spectre"
    ? "Default" | "Green" | "Purple" | "Orange"
    :
    T extends "Spline Spectre"
    ? "Default" | "Green" | "Red" | "Blue"
    :
    T extends "Minima Spectre"
    ? "Default"
    :
    T extends "Kuronami Spectre"
    ? "Default" | "Purple" | "White" | "Black"
    :
    T extends "Ion Spectre"
    ? "Default" | "Green" | "Yellow" | "Blue"
    :
    T extends "Hue Shift Spectre"
    ? "Default"
    :
    T extends "Aero Spectre"
    ? "Default" | "Cream/Pink" | "Purple/Gray" | "Green/Black"
    :
    T extends "POLYfrog Spectre"
    ? "Default"
    :
    T extends "Protocol 781-A Spectre"
    ? "Default" | "White/Pink" | "Red/Blue" | "Green"
    :
    T extends "Luna Spectre"
    ? "Default"
    :
    T extends "Gravitational Uranium Neuroblaster Spectre"
    ? "Default" | "Chrome" | "Black" | "Red/Blue"
    :
    T extends "Sentinels of Light Spectre"
    ? "Default" | "Pink" | "Red/Green" | "Blue/Purple"
    :
    T extends "Shellspire Spectre"
    ? "Default"
    :
    T extends "Torque Spectre"
    ? "Default" | "Yellow" | "Blue" | "Orange"
    :
    T extends "Helix Spectre"
    ? "Default" | "Gold" | "Purple" | "Silver"
    :
    T extends "Reaver Spectre"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "Recon Spectre"
    ? "Default" | "RedCamo" | "BlueCamo" | "GreenCamo"
    :
    T extends "Infinity Spectre"
    ? "Default" | "White" | "Yellow" | "Pink"
    :
    T extends "Standard Spectre"
    ? "Default"
    :
    T extends "Evori Dreamwings Spectre"
    ? "Default" | "Pink" | "Orange" | "Blue"
    :
    T extends "Freehand Spectre"
    ? "Default"
    :
    T extends "Tigris Spectre"
    ? "Default"
    :
    T extends "NO LIMITS Spectre"
    ? "Default"
    :
    T extends "Sandswept Spectre"
    ? "Default" | "Ivory" | "Indigo" | "Scarlet"
    :
    T extends "Convex Spectre"
    ? "Default"
    :
    T extends "BlastX Spectre"
    ? "Default" | "Black" | "Yellow" | "Pink"
    :
    T extends "Wonderstallion Spectre"
    ? "Default"
    :
    T extends "Solarex Spectre"
    ? "Default" | "Yellow" | "Green" | "Purple"
    :
    T extends "Wasteland Spectre"
    ? "Default"
    :
  unknown;
