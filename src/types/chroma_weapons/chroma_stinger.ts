/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type StingerChromaType<T> =   T extends "Immortalized Stinger"
    ? "Default" | "Orange" | "Blue" | "Green"
    :
    T extends "RGX 11z Pro Stinger"
    ? "Default" | "Red" | "Blue" | "Yellow"
    :
    T extends "Aristocrat Stinger"
    ? "Default"
    :
    T extends "Celestia Stinger"
    ? "Default" | "Silver" | "RoseGold" | "Gold"
    :
    T extends "Composite Stinger"
    ? "Default"
    :
    T extends "Overlay Stinger"
    ? "Default" | "Black" | "Pink" | "Grey"
    :
    T extends "Tactiplay Stinger"
    ? "Default"
    :
    T extends "Red Alert Stinger"
    ? "Default"
    :
    T extends "Prelude to Chaos Stinger"
    ? "Default" | "Green" | "White" | "Blue"
    :
    T extends "Doodle Buds Stinger"
    ? "Default" | "Tactifriends" | "LeagueofLegends"
    :
    T extends "Surge Stinger"
    ? "Default" | "Black" | "Yellow" | "Blue"
    :
    T extends "Shimmer Stinger"
    ? "Default" | "Gold" | "Red" | "Teal"
    :
    T extends ".SYS Stinger"
    ? "Default"
    :
    T extends "Prism II Stinger"
    ? "Default"
    :
    T extends "Aquatica Stinger"
    ? "Default"
    :
    T extends "Couture Stinger"
    ? "Default"
    :
    T extends "Signature Stinger"
    ? "Default"
    :
    T extends "Overdrive Stinger"
    ? "Default" | "Cobalt" | "Bubblegum" | "Onyx"
    :
    T extends "Silvanus Stinger"
    ? "Default"
    :
    T extends "Moondash Stinger"
    ? "Default"
    :
    T extends "Libretto Stinger"
    ? "Default"
    :
    T extends "Switchback Stinger"
    ? "Default"
    :
    T extends "Sensation Stinger"
    ? "Default"
    :
    T extends "Sakura Stinger"
    ? "Default"
    :
    T extends "Schema Stinger"
    ? "Default"
    :
    T extends "Lycan's Bane Stinger"
    ? "Default"
    :
    T extends "Transition Stinger"
    ? "Default" | "Blue" | "Orange" | "Green"
    :
    T extends "Sovereign Stinger"
    ? "Default" | "Green" | "Silver" | "Purple"
    :
    T extends "Cavalier Stinger"
    ? "Default"
    :
    T extends "Standard Stinger"
    ? "Default"
    :
    T extends "Refractrix Stinger"
    ? "Default"
    :
    T extends "CYRAX Stinger"
    ? "Default" | "Red" | "Yellow" | "Purple"
    :
    T extends "Gridcrash Stinger"
    ? "Default"
    :
    T extends "Cloudweaver Stinger"
    ? "Default"
    :
    T extends "Depths Stinger"
    ? "Default"
    :
    T extends "Ego Stinger"
    ? "Default" | "Red" | "Tan" | "Pink"
    :
    T extends "Varnish Stinger"
    ? "Default"
    :
    T extends "Storm Maw Stinger"
    ? "Default"
    :
    T extends "Convergence Stinger"
    ? "Default"
    :
  unknown;
