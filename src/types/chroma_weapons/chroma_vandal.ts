/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type VandalChromaType<T> =   T extends "Immortalized Vandal"
    ? "Default" | "Orange" | "Blue" | "Green"
    :
    T extends "RGX 11z Pro Vandal"
    ? "Default" | "Red" | "Blue" | "Yellow"
    :
    T extends "Altitude Vandal"
    ? "Default"
    :
    T extends "VALORANT GO! Vol. 2 Vandal"
    ? "Default"
    :
    T extends "Divergence Vandal"
    ? "Default" | "Purple" | "Blue" | "Green"
    :
    T extends "Araxys Vandal"
    ? "Default" | "Purple" | "Black" | "Silver"
    :
    T extends "Neptune Vandal"
    ? "Default" | "Black"
    :
    T extends "Aristocrat Vandal"
    ? "Default"
    :
    T extends "Gaia's Vengeance Vandal"
    ? "Default" | "Blue" | "Green" | "Orange"
    :
    T extends "MK.VII Liberty Vandal"
    ? "Default" | "Desert" | "Sea" | "Tundra"
    :
    T extends "Endeavour Vandal"
    ? "Default"
    :
    T extends "Champions 2021 Vandal"
    ? "Default"
    :
    T extends "Champions 2023 Vandal"
    ? "Default"
    :
    T extends "Origin Vandal"
    ? "Default" | "Green" | "Red" | "White"
    :
    T extends "Comet Vandal"
    ? "Default" | "Red" | "Pink" | "Yellow"
    :
    T extends "Starlit Odyssey Vandal"
    ? "Default" | "Twilight" | "Sunset" | "Midnight"
    :
    T extends "Glitchpop Vandal"
    ? "Default" | "Blue" | "Red" | "Gold"
    :
    T extends "ChronoVoid Vandal"
    ? "Default" | "Purple" | "Red" | "Black"
    :
    T extends "Prelude to Chaos Vandal"
    ? "Default" | "Green" | "White" | "Blue"
    :
    T extends "Elderflame Vandal"
    ? "Default" | "Red" | "Blue" | "Dark"
    :
    T extends "Imperium Vandal"
    ? "Default" | "Ruby" | "Pearl" | "Obsidian"
    :
    T extends "Singularity Vandal"
    ? "Default" | "Red" | "White" | "Blue"
    :
    T extends "Sarmad Vandal"
    ? "Default"
    :
    T extends "Nitro Vandal"
    ? "Default"
    :
    T extends "Hivemind Vandal"
    ? "Default"
    :
    T extends "Venturi Vandal"
    ? "Default"
    :
    T extends "Forsaken Vandal"
    ? "Default" | "Gold"
    :
    T extends "Team Ace Vandal"
    ? "Default"
    :
    T extends "Ruin Vandal"
    ? "Default"
    :
    T extends ".EXE Vandal"
    ? "Default"
    :
    T extends ".SYS Vandal"
    ? "Default"
    :
    T extends "Primordium Vandal"
    ? "Default" | "Venom" | "Cobalt" | "Gold"
    :
    T extends "EX.O Vandal"
    ? "Default" | "Yellow" | "Red" | "Pink"
    :
    T extends "Horizon Vandal"
    ? "Default"
    :
    T extends "Prime Vandal"
    ? "Default" | "Orange" | "Blue" | "Yellow"
    :
    T extends "XERØFANG Vandal "
    ? "Default" | "Black" | "Pink" | "Silver"
    :
    T extends "Avalanche Vandal"
    ? "Default"
    :
    T extends "Bubble Pop Vandal"
    ? "Default" | "Red"
    :
    T extends "Guardrail Vandal"
    ? "Default"
    :
    T extends "Black.Market Vandal"
    ? "Default"
    :
    T extends "Prism II Vandal"
    ? "Default"
    :
    T extends "K/TAC Vandal"
    ? "Default"
    :
    T extends "Aemondir Vandal"
    ? "Default" | "Silver" | "Bronze" | "Obsidian"
    :
    T extends "Smite Vandal"
    ? "Default"
    :
    T extends "Monstrocity Vandal"
    ? "Default" | "Brown" | "Blue" | "Yellow"
    :
    T extends "Nunca Olvidados Vandal"
    ? "Default" | "Living" | "Eternal"
    :
    T extends "Luxe Vandal"
    ? "Default"
    :
    T extends "Arcane Vandal"
    ? "Default"
    :
    T extends "Magepunk Vandal"
    ? "Default" | "Green" | "Black/Red" | "Yellow"
    :
    T extends "Minima Vandal"
    ? "Default"
    :
    T extends "Valiant Hero Vandal"
    ? "Default" | "Sunset" | "Dusk" | "Night"
    :
    T extends "Overdrive Vandal"
    ? "Default" | "Cobalt" | "Bubblegum" | "Onyx"
    :
    T extends "Silvanus Vandal"
    ? "Default"
    :
    T extends "Crimsonbeast Vandal"
    ? "Default"
    :
    T extends "Kuronami Vandal"
    ? "Default" | "Purple" | "White" | "Black"
    :
    T extends "Ion Vandal"
    ? "Default" | "Green" | "Yellow" | "Blue"
    :
    T extends "Oni Vandal"
    ? "Default" | "Kumo" | "Hana" | "Tsubame"
    :
    T extends "Cryostasis Vandal"
    ? "Default"
    :
    T extends "Luna Vandal"
    ? "Default"
    :
    T extends "Sensation Vandal"
    ? "Default"
    :
    T extends "Orion Vandal"
    ? "Default"
    :
    T extends "Sentinels of Light Vandal"
    ? "Default" | "Pink" | "Red/Green" | "Blue/Purple"
    :
    T extends "Sakura Vandal"
    ? "Default"
    :
    T extends "Schema Vandal"
    ? "Default"
    :
    T extends "Lycan's Bane Vandal"
    ? "Default"
    :
    T extends "Transition Vandal"
    ? "Default" | "Blue" | "Orange" | "Green"
    :
    T extends "Byteshift Vandal"
    ? "Default" | "Purple" | "Red" | "Yellow"
    :
    T extends "Torque Vandal"
    ? "Default" | "Yellow" | "Blue" | "Orange"
    :
    T extends "Reaver Vandal"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "Tethered Realms Vandal"
    ? "Default"
    :
    T extends "Cavalier Vandal"
    ? "Default"
    :
    T extends "Titanmail Vandal"
    ? "Default"
    :
    T extends "Standard Vandal"
    ? "Default"
    :
    T extends "Evori Dreamwings Vandal"
    ? "Default" | "Green" | "Pink" | "Orange"
    :
    T extends "CYRAX Vandal"
    ? "Default" | "Red" | "Yellow" | "Purple"
    :
    T extends "NO LIMITS Vandal"
    ? "Default"
    :
    T extends "Sandswept Vandal"
    ? "Default" | "Ivory" | "Indigo" | "Scarlet"
    :
    T extends "Depths Vandal"
    ? "Default"
    :
    T extends "Wonderstallion Vandal"
    ? "Default"
    :
    T extends "Ego Vandal"
    ? "Default" | "Red" | "Tan" | "Pink"
    :
    T extends "Holomoku Vandal"
    ? "Default" | "DeepSea"
    :
    T extends "Wasteland Vandal"
    ? "Default"
    :
    T extends "Winterwunderland Vandal"
    ? "Default"
    :
    T extends "Storm Maw Vandal"
    ? "Default"
    :
  unknown;
