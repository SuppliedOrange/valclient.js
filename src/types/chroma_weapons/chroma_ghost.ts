/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type GhostChromaType<T> =   T extends "Flutter Ghost"
    ? "Default"
    :
    T extends "Fiber Optic Ghost"
    ? "Default" | "Silver" | "Gold" | "Emerald"
    :
    T extends "Frequency Ghost"
    ? "Default"
    :
    T extends "VALORANT GO! Vol. 1 Ghost"
    ? "Default"
    :
    T extends "Radiant Entertainment System Ghost"
    ? "Default" | "DanceFever" | "K.nockO.ut!!"
    :
    T extends "Gaia's Vengeance Ghost"
    ? "Default" | "Blue" | "Green" | "Orange"
    :
    T extends "Celestia Ghost"
    ? "Default" | "Silver" | "RoseGold" | "Gold"
    :
    T extends "Endeavour Ghost"
    ? "Default"
    :
    T extends "Piedra del Sol Ghost"
    ? "Default"
    :
    T extends "Bumble Brigade Ghost"
    ? "Default"
    :
    T extends "Aperture Ghost"
    ? "Default"
    :
    T extends "Tacti-Treat Ghost"
    ? "Default"
    :
    T extends "Comet Ghost"
    ? "Default" | "Red" | "Pink" | "Yellow"
    :
    T extends "Starlit Odyssey Ghost"
    ? "Default" | "Twilight" | "Sunset" | "Midnight"
    :
    T extends "Singularity Ghost"
    ? "Default" | "Red" | "White" | "Blue"
    :
    T extends "Topotek Ghost"
    ? "Default"
    :
    T extends "Spitfire Ghost"
    ? "Default"
    :
    T extends ".EXE Ghost"
    ? "Default"
    :
    T extends "Hush Ghost"
    ? "Default"
    :
    T extends "Soulstrife Ghost"
    ? "Default"
    :
    T extends "Digihex Ghost"
    ? "Default" | "Red" | "Green" | "Gold"
    :
    T extends "Infantry Ghost"
    ? "Default"
    :
    T extends "Goldwing Ghost"
    ? "Default"
    :
    T extends "XERØFANG Ghost"
    ? "Default" | "Black" | "Pink" | "Silver"
    :
    T extends "Outpost Ghost"
    ? "Default"
    :
    T extends "Prism Ghost"
    ? "Default"
    :
    T extends "Serenity Ghost"
    ? "Default"
    :
    T extends "Ruination Ghost"
    ? "Default" | "Purple" | "Red/Yellow" | "Green"
    :
    T extends "Smite Ghost"
    ? "Default"
    :
    T extends "Luxe Ghost"
    ? "Default"
    :
    T extends "Magepunk Ghost"
    ? "Default" | "Green" | "Purple" | "Orange"
    :
    T extends "Minima Ghost"
    ? "Default"
    :
    T extends "Valiant Hero Ghost"
    ? "Default" | "Sunset" | "Dusk" | "Night"
    :
    T extends "Libretto Ghost"
    ? "Default"
    :
    T extends "Artisan Ghost"
    ? "Default" | "Red" | "Green" | "Blue"
    :
    T extends "Jigsaw Ghost"
    ? "Default" | "Skye" | "Killjoy" | "Astra"
    :
    T extends "Luna Ghost"
    ? "Default"
    :
    T extends "Eclipse Ghost"
    ? "Default"
    :
    T extends "Lycan's Bane Ghost"
    ? "Default"
    :
    T extends "Byteshift Ghost"
    ? "Default" | "Purple" | "Red" | "Yellow"
    :
    T extends "Reaver Ghost"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "Tethered Realms Ghost"
    ? "Default"
    :
    T extends "Sovereign Ghost"
    ? "Default" | "Green" | "Silver" | "Purple"
    :
    T extends "Recon Ghost"
    ? "Default" | "RedCamo" | "BlueCamo" | "GreenCamo"
    :
    T extends "Cavalier Ghost"
    ? "Default"
    :
    T extends "Standard Ghost"
    ? "Default"
    :
    T extends "Evori Dreamwings Ghost"
    ? "Default" | "Orange" | "Blue" | "Green"
    :
    T extends "Freehand Ghost"
    ? "Default"
    :
    T extends "CYRAX Ghost"
    ? "Default" | "Red" | "Yellow" | "Purple"
    :
    T extends "Gridcrash Ghost"
    ? "Default"
    :
    T extends "Fortune's Hand Ghost"
    ? "Default"
    :
    T extends "NO LIMITS Ghost"
    ? "Default"
    :
    T extends "Depths Ghost"
    ? "Default"
    :
    T extends "Ego Ghost"
    ? "Default" | "Red" | "Tan" | "Pink"
    :
    T extends "Vendetta Ghost"
    ? "Default"
    :
    T extends "Winterwunderland Ghost"
    ? "Default"
    :
    T extends "Soul Silencer Ghost"
    ? "Default"
    :
  unknown;
