/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type FrenzyChromaType<T> =   T extends "Task Force 809 Frenzy"
    ? "Default"
    :
    T extends "Kaleidoscope Frenzy"
    ? "Default"
    :
    T extends "RGX 11z Pro Frenzy"
    ? "Default" | "Red" | "Blue" | "Yellow"
    :
    T extends "Xenohunter Frenzy"
    ? "Default"
    :
    T extends "Bolt Frenzy"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "RagnaRocker Frenzy"
    ? "Default"
    :
    T extends "Origin Frenzy"
    ? "Default" | "Green" | "Red" | "White"
    :
    T extends "Bulletbox Frenzy"
    ? "Default"
    :
    T extends "Glitchpop Frenzy"
    ? "Default" | "Blue" | "Red" | "Gold"
    :
    T extends "Emberclad Frenzy"
    ? "Default"
    :
    T extends "Elderflame Frenzy"
    ? "Default" | "Red" | "Blue" | "Dark"
    :
    T extends "Sarmad Frenzy"
    ? "Default"
    :
    T extends "Rush Frenzy"
    ? "Default"
    :
    T extends "Venturi Frenzy"
    ? "Default"
    :
    T extends "Team Ace Frenzy"
    ? "Default"
    :
    T extends "Swooping Frenzy"
    ? "Default"
    :
    T extends "Hieroscape Frenzy"
    ? "Default"
    :
    T extends "Horizon Frenzy"
    ? "Default"
    :
    T extends "Monarch Frenzy"
    ? "Default"
    :
    T extends "Hydrodip Frenzy"
    ? "Default"
    :
    T extends "Prime//2.0 Frenzy"
    ? "Default" | "Gold" | "Green" | "Orange"
    :
    T extends "Guardrail Frenzy"
    ? "Default"
    :
    T extends "Couture Frenzy"
    ? "Default"
    :
    T extends "Blush Frenzy"
    ? "Default"
    :
    T extends "Nunca Olvidados Frenzy"
    ? "Default" | "Living" | "Eternal"
    :
    T extends "Celestial Frenzy"
    ? "Default"
    :
    T extends "Tacti-Series Frenzy"
    ? "Default"
    :
    T extends "Coalition: Cobra Frenzy"
    ? "Default" | "Blue" | "Red" | "Orange"
    :
    T extends "Combat Crafts Frenzy"
    ? "Default"
    :
    T extends "Moondash Frenzy"
    ? "Default"
    :
    T extends "Ion Frenzy"
    ? "Default" | "Green" | "Yellow" | "Blue"
    :
    T extends "Oni Frenzy"
    ? "Default" | "Kumo" | "Hana" | "Tsubame"
    :
    T extends "Spitfire Frenzy"
    ? "Default"
    :
    T extends "Aero Frenzy"
    ? "Default" | "Cream/Pink" | "Purple/Gray" | "Green/Black"
    :
    T extends "Divine Swine Frenzy"
    ? "Default"
    :
    T extends "Lightwave Frenzy"
    ? "Default" | "Blue" | "Red" | "Gray"
    :
    T extends "Sensation Frenzy"
    ? "Default"
    :
    T extends "Orion Frenzy"
    ? "Default"
    :
    T extends "Silhouette Frenzy"
    ? "Default"
    :
    T extends "Shellspire Frenzy"
    ? "Default"
    :
    T extends "Sovereign Frenzy"
    ? "Default" | "Green" | "Teal" | "Red"
    :
    T extends "Titanmail Frenzy"
    ? "Default"
    :
    T extends "Live Wire Frenzy"
    ? "Default"
    :
    T extends "Standard Frenzy"
    ? "Default"
    :
    T extends "Spellbound Frenzy"
    ? "Default"
    :
    T extends "BlastX Frenzy"
    ? "Default" | "Black" | "Yellow" | "Pink"
    :
    T extends "Wonderstallion Frenzy"
    ? "Default"
    :
    T extends "Holomoku Frenzy"
    ? "Default" | "DeepSea"
    :
    T extends "Convergence Frenzy"
    ? "Default"
    :
  unknown;
