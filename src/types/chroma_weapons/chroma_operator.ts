/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type OperatorChromaType<T> =   T extends "RGX 11z Pro Operator"
    ? "Default" | "Red" | "Blue" | "Yellow"
    :
    T extends "Frequency Operator"
    ? "Default"
    :
    T extends "Intergrade Operator"
    ? "Default"
    :
    T extends "VALORANT GO! Vol. 2 Operator"
    ? "Default"
    :
    T extends "Divergence Operator"
    ? "Default" | "Purple" | "Blue" | "Green"
    :
    T extends "Araxys Operator"
    ? "Default" | "Purple" | "Black" | "Silver"
    :
    T extends "Radiant Entertainment System Operator"
    ? "Default" | "DanceFever" | "K.nockO.ut!!"
    :
    T extends "MK.VII Liberty Operator "
    ? "Default" | "Desert" | "Sea" | "Tundra"
    :
    T extends "Celestia Operator"
    ? "Default" | "Silver" | "RoseGold" | "Gold"
    :
    T extends "Endeavour Operator"
    ? "Default"
    :
    T extends "Tacti-Treat Operator"
    ? "Default"
    :
    T extends "Origin Operator"
    ? "Default" | "Green" | "Red" | "White"
    :
    T extends "Striker Operator"
    ? "Default" | "Green/Yellow/Black" | "Pink/Teal/Purple" | "Blue/White/Red"
    :
    T extends "Red Alert Operator"
    ? "Default"
    :
    T extends "Glitchpop Operator"
    ? "Default" | "Blue" | "Red" | "Gold"
    :
    T extends "Prelude to Chaos Operator"
    ? "Default" | "Green" | "White" | "Blue"
    :
    T extends "Elderflame Operator"
    ? "Default" | "Red" | "Blue" | "Dark"
    :
    T extends "Imperium Operator"
    ? "Default" | "Ruby" | "Pearl" | "Obsidian"
    :
    T extends "Nitro Operator"
    ? "Default"
    :
    T extends "Forsaken Operator"
    ? "Default" | "Gold"
    :
    T extends "Team Ace Operator"
    ? "Default"
    :
    T extends "Spitfire Operator"
    ? "Default"
    :
    T extends "Infantry Operator"
    ? "Default"
    :
    T extends "Prism Operator"
    ? "Default"
    :
    T extends "Retrowave Operator"
    ? "Default" | "Navy" | "Gold" | "Violet"
    :
    T extends "K/TAC Operator"
    ? "Default"
    :
    T extends "Kohaku & Matsuba Operator"
    ? "Default" | "Dark"
    :
    T extends "Blush Operator"
    ? "Default"
    :
    T extends "Daydreams Operator"
    ? "Default"
    :
    T extends "Iridian Thorn Operator"
    ? "Default"
    :
    T extends "Luxe Operator"
    ? "Default"
    :
    T extends "Magepunk Operator"
    ? "Default" | "Green" | "Pink" | "Orange"
    :
    T extends "Spline Operator"
    ? "Default" | "Green" | "Red" | "Blue"
    :
    T extends "Minima Operator"
    ? "Default"
    :
    T extends "Valiant Hero Operator"
    ? "Default" | "Sunset" | "Dusk" | "Night"
    :
    T extends "Silvanus Operator"
    ? "Default"
    :
    T extends "Genesis Operator"
    ? "Default"
    :
    T extends "Libretto Operator"
    ? "Default"
    :
    T extends "Ion Operator"
    ? "Default"
    :
    T extends "Cryostasis Operator"
    ? "Default"
    :
    T extends "Gravitational Uranium Neuroblaster Operator"
    ? "Default" | "Chrome" | "Black" | "Red/Blue"
    :
    T extends "Sentinels of Light Operator"
    ? "Default" | "Pink" | "Red/Green" | "Blue/Purple"
    :
    T extends "Torque Operator"
    ? "Default" | "Yellow" | "Blue" | "Orange"
    :
    T extends "Reaver Operator"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "Tethered Realms Operator"
    ? "Default"
    :
    T extends "Cavalier Operator"
    ? "Default"
    :
    T extends "Mystbloom Operator"
    ? "Default" | "Blue" | "Orange" | "Purple"
    :
    T extends "Standard Operator"
    ? "Default"
    :
    T extends "Aerosol Operator"
    ? "Default"
    :
    T extends "Tilde Operator"
    ? "Default" | "Red" | "Purple" | "Green"
    :
    T extends "Tigris Operator"
    ? "Default"
    :
    T extends "Convex Operator"
    ? "Default"
    :
  unknown;
