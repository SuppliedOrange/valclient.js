/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type BulldogChromaType<T> =   T extends "Frequency Bulldog"
    ? "Default"
    :
    T extends "Araxys Bulldog"
    ? "Default" | "Purple" | "Black" | "Silver"
    :
    T extends "Neptune Bulldog"
    ? "Default" | "Black"
    :
    T extends "Radiant Entertainment System Bulldog"
    ? "Default" | "DanceFever" | "K.nockO.ut!!"
    :
    T extends "Aristocrat Bulldog"
    ? "Default"
    :
    T extends "Endeavour Bulldog"
    ? "Default"
    :
    T extends "Spectrum Bulldog"
    ? "Default" | "Black" | "Red" | "Purple/Pink"
    :
    T extends "Bumble Brigade Bulldog"
    ? "Default"
    :
    T extends "Aperture Bulldog"
    ? "Default"
    :
    T extends "Tactiplay Bulldog"
    ? "Default"
    :
    T extends "Striker Bulldog"
    ? "Default" | "Green/Yellow/Black" | "Pink/Teal/Purple" | "Blue/White/Red"
    :
    T extends "Rune Stone Bulldog"
    ? "Default"
    :
    T extends "Bulletbox Bulldog"
    ? "Default"
    :
    T extends "Nocturnum Bulldog"
    ? "Default" | "Black" | "Blue" | "Yellow"
    :
    T extends "Undercity Bulldog"
    ? "Default"
    :
    T extends "Glitchpop Bulldog"
    ? "Default" | "Blue" | "Red" | "Gold"
    :
    T extends "Emberclad Bulldog"
    ? "Default"
    :
    T extends "Rush Bulldog"
    ? "Default"
    :
    T extends "Chromedek Bulldog"
    ? "Default"
    :
    T extends "Velocity Bulldog"
    ? "Default" | "Yellow" | "Purple" | "Aquamarine"
    :
    T extends "Shimmer Bulldog"
    ? "Default" | "Gold" | "Red" | "Teal"
    :
    T extends "Digihex Bulldog"
    ? "Default" | "Red" | "Green" | "Gold"
    :
    T extends "Hieroscape Bulldog"
    ? "Default"
    :
    T extends "Horizon Bulldog"
    ? "Default"
    :
    T extends "Black.Market Bulldog"
    ? "Default"
    :
    T extends "Bound Bulldog"
    ? "Default"
    :
    T extends "Retrowave Bulldog"
    ? "Default" | "Navy" | "Gold" | "Violet"
    :
    T extends "K/TAC Bulldog"
    ? "Default"
    :
    T extends "Couture Bulldog"
    ? "Default"
    :
    T extends "Aemondir Bulldog"
    ? "Default" | "Silver" | "Bronze" | "Obsidian"
    :
    T extends "Nunca Olvidados Bulldog"
    ? "Default" | "Living" | "Eternal"
    :
    T extends "Minima Bulldog"
    ? "Default"
    :
    T extends "Combat Crafts Bulldog"
    ? "Default"
    :
    T extends "Genesis Bulldog"
    ? "Default"
    :
    T extends "Libretto Bulldog"
    ? "Default"
    :
    T extends "Oni Bulldog"
    ? "Default" | "Kumo" | "Hana" | "Tsubame"
    :
    T extends "Cryostasis Bulldog"
    ? "Default"
    :
    T extends "Hue Shift Bulldog"
    ? "Default"
    :
    T extends "POLYfox Bulldog"
    ? "Default"
    :
    T extends "Protocol 781-A Bulldog"
    ? "Default" | "White/Pink" | "Red/Blue" | "Green"
    :
    T extends "Premiere Collision Bulldog"
    ? "Default"
    :
    T extends "Transition Bulldog"
    ? "Default" | "Blue" | "Orange" | "Green"
    :
    T extends "Infinity Bulldog"
    ? "Default" | "White" | "Yellow" | "Pink"
    :
    T extends "Standard Bulldog"
    ? "Default"
    :
    T extends "Spellbound Bulldog"
    ? "Default"
    :
    T extends "Tilde Bulldog"
    ? "Default" | "Red" | "Purple" | "Green"
    :
    T extends "Gridcrash Bulldog"
    ? "Default"
    :
    T extends "NO LIMITS Bulldog"
    ? "Default"
    :
    T extends "Convex Bulldog"
    ? "Default"
    :
    T extends "Depths Bulldog"
    ? "Default"
    :
    T extends "Holomoku Bulldog"
    ? "Default" | "DeepSea"
    :
    T extends "Varnish Bulldog"
    ? "Default"
    :
  unknown;
