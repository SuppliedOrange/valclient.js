/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type GuardianChromaType<T> =   T extends "Immortalized Guardian"
    ? "Default" | "Orange" | "Blue" | "Green"
    :
    T extends "RGX 11z Pro Guardian"
    ? "Default" | "Red" | "Blue" | "Yellow"
    :
    T extends "Intergrade Guardian"
    ? "Default"
    :
    T extends "VALORANT GO! Vol. 1 Guardian"
    ? "Default"
    :
    T extends "Araxys Guardian"
    ? "Default" | "Purple" | "Black" | "Silver"
    :
    T extends "Neptune Guardian"
    ? "Default" | "Black"
    :
    T extends "Gaia's Vengeance Guardian"
    ? "Default" | "Blue" | "Green" | "Orange"
    :
    T extends "MK.VII Liberty Guardian"
    ? "Default" | "Desert" | "Sea" | "Tundra"
    :
    T extends "Spectrum Guardian"
    ? "Default" | "Black" | "Red" | "Purple/Pink"
    :
    T extends "Tacti-Treat Guardian"
    ? "Default"
    :
    T extends "Starlit Odyssey Guardian"
    ? "Default" | "Twilight" | "Sunset" | "Midnight"
    :
    T extends "Nebula Guardian"
    ? "Default"
    :
    T extends "Bulletbox Guardian"
    ? "Default"
    :
    T extends "Abyssal Guardian"
    ? "Default"
    :
    T extends "Nitro Guardian"
    ? "Default"
    :
    T extends "Songsteel Guardian"
    ? "Default"
    :
    T extends "Moon Scout Guardian"
    ? "Default"
    :
    T extends "Spitfire Guardian"
    ? "Default"
    :
    T extends "Ruin Guardian"
    ? "Default"
    :
    T extends "Soulstrife Guardian"
    ? "Default"
    :
    T extends "Infantry Guardian"
    ? "Default"
    :
    T extends "Hydrodip Guardian"
    ? "Default"
    :
    T extends "Prime Guardian"
    ? "Default" | "Orange" | "Blue" | "Yellow"
    :
    T extends "Bubble Pop Guardian"
    ? "Default" | "Red"
    :
    T extends "Guardrail Guardian"
    ? "Default"
    :
    T extends "Yoonseul Guardian"
    ? "Default"
    :
    T extends "Ruination Guardian"
    ? "Default" | "Purple" | "Red/Yellow" | "Green"
    :
    T extends "Blush Guardian"
    ? "Default"
    :
    T extends "Magepunk Guardian"
    ? "Default" | "Green" | "Pink" | "Orange"
    :
    T extends "Tacti-Series Guardian"
    ? "Default"
    :
    T extends "Signature Guardian"
    ? "Default"
    :
    T extends "Galleria Guardian"
    ? "Default"
    :
    T extends "Moondash Guardian"
    ? "Default"
    :
    T extends "Oni Guardian"
    ? "Default" | "Kumo" | "Hana" | "Tsubame"
    :
    T extends "9 Lives Guardian"
    ? "Default" | "Raze" | "Reyna" | "Viper"
    :
    T extends "Aero Guardian"
    ? "Default" | "Cream/Pink" | "Purple/Gray" | "Green/Black"
    :
    T extends "POLYfox Guardian"
    ? "Default"
    :
    T extends "Jigsaw Guardian"
    ? "Default" | "Skye" | "Killjoy" | "Yoru"
    :
    T extends "Panoramic Guardian"
    ? "Default"
    :
    T extends "Silhouette Guardian"
    ? "Default"
    :
    T extends "Shellspire Guardian"
    ? "Default"
    :
    T extends "Reaver Guardian"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "Tethered Realms Guardian"
    ? "Default"
    :
    T extends "Sovereign Guardian"
    ? "Default" | "Green" | "Silver" | "Purple"
    :
    T extends "Recon Guardian"
    ? "Default" | "RedCamo" | "BlueCamo" | "GreenCamo"
    :
    T extends "Infinity Guardian"
    ? "Default" | "White" | "Yellow" | "Pink"
    :
    T extends "Reverie Guardian"
    ? "Default"
    :
    T extends "Standard Guardian"
    ? "Default"
    :
    T extends "Refractrix Guardian"
    ? "Default"
    :
    T extends "CYRAX Guardian"
    ? "Default" | "Red" | "Yellow" | "Purple"
    :
    T extends "Fortune's Hand Guardian"
    ? "Default"
    :
    T extends "Wonderstallion Guardian"
    ? "Default"
    :
    T extends "Ego Guardian"
    ? "Default" | "Red" | "Tan" | "Pink"
    :
  unknown;
