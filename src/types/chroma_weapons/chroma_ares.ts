/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type AresChromaType<T> =   T extends "VALORANT GO! Vol. 2 Ares"
    ? "Default"
    :
    T extends "Aristocrat Ares"
    ? "Default"
    :
    T extends "Gaia's Vengeance Ares"
    ? "Default" | "Blue" | "Green" | "Orange"
    :
    T extends "Endeavour Ares"
    ? "Default"
    :
    T extends "Bumble Brigade Ares"
    ? "Default"
    :
    T extends "Nebula Ares"
    ? "Default"
    :
    T extends "Doodle Buds Ares"
    ? "Default" | "Tactifriends" | "LeagueofLegends"
    :
    T extends "Singularity Ares"
    ? "Default" | "Blue" | "Red" | "Purple"
    :
    T extends "Rush Ares"
    ? "Default"
    :
    T extends "Hivemind Ares"
    ? "Default"
    :
    T extends "Spitfire Ares"
    ? "Default"
    :
    T extends "Digihex Ares"
    ? "Default" | "Red" | "Green" | "Gold"
    :
    T extends "Hieroscape Ares"
    ? "Default"
    :
    T extends "Infantry Ares"
    ? "Default"
    :
    T extends "Goldwing Ares"
    ? "Default"
    :
    T extends "Outpost Ares"
    ? "Default"
    :
    T extends "Prism Ares"
    ? "Default"
    :
    T extends "Aquatica Ares"
    ? "Default"
    :
    T extends "Yoonseul Ares"
    ? "Default"
    :
    T extends "Monstrocity Ares"
    ? "Default" | "Brown" | "Blue" | "Yellow"
    :
    T extends "Nunca Olvidados Ares"
    ? "Default" | "Living" | "Eternal"
    :
    T extends "Celestial Ares"
    ? "Default"
    :
    T extends "Magepunk Ares"
    ? "Default" | "Green" | "Pink" | "Orange"
    :
    T extends "Minima Ares"
    ? "Default"
    :
    T extends "Valiant Hero Ares"
    ? "Default" | "Sunset" | "Dusk" | "Night"
    :
    T extends "Ion Ares"
    ? "Default" | "Green" | "Yellow" | "Blue"
    :
    T extends "Oni Ares"
    ? "Default" | "Kumo" | "Hana" | "Tsubame"
    :
    T extends "9 Lives Ares"
    ? "Default" | "Raze" | "Reyna" | "Viper"
    :
    T extends "POLYfrog Ares"
    ? "Default"
    :
    T extends "Divine Swine Ares"
    ? "Default"
    :
    T extends "Jigsaw Ares"
    ? "Default" | "Killjoy" | "Astra" | "Yoru"
    :
    T extends "Premiere Collision Ares"
    ? "Default"
    :
    T extends "Sentinels of Light Ares"
    ? "Default" | "Pink" | "Red/Green" | "Blue/Purple"
    :
    T extends "Sakura Ares"
    ? "Default"
    :
    T extends "Silhouette Ares"
    ? "Default"
    :
    T extends "Snowfall Ares"
    ? "Default"
    :
    T extends "Titanmail Ares"
    ? "Default"
    :
    T extends "Standard Ares"
    ? "Default"
    :
  unknown;
