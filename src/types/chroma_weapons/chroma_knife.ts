/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type KnifeChromaType<T> =   T extends "Task Force 809 Knife"
    ? "Default"
    :
    T extends "RGX 11z Pro Blade"
    ? "Default" | "Red" | "Blue" | "Yellow"
    :
    T extends "RGX 11z Pro Firefly"
    ? "Default" | "Red" | "Blue" | "Yellow"
    :
    T extends "RGX 11z Pro Karambit"
    ? "Default" | "Red" | "Blue" | "Yellow"
    :
    T extends "Altitude Knuckle Knife"
    ? "Default"
    :
    T extends "Xenohunter Knife"
    ? "Default"
    :
    T extends "Intergrade Blade"
    ? "Default"
    :
    T extends "VALORANT GO! Vol. 1 Knife"
    ? "Default"
    :
    T extends "Yoru's Stylish Butterfly Comb"
    ? "Default"
    :
    T extends "Divergence Staff"
    ? "Default" | "Purple" | "Blue" | "Green"
    :
    T extends "Araxys Bio Harvester"
    ? "Default" | "Purple" | "Black" | "Silver"
    :
    T extends "Araxys Bio-Atomizers"
    ? "Default" | "Purple" | "Black" | "Silver"
    :
    T extends "Neptune Anchor"
    ? "Default"
    :
    T extends "Neptune Hook"
    ? "Default" | "Black"
    :
    T extends "Power Fist"
    ? "Default" | "DanceFever" | "K.nockO.ut!!"
    :
    T extends "Gaia's Wrath"
    ? "Default" | "Blue" | "Green" | "Orange"
    :
    T extends "Gaia's Fury"
    ? "Default" | "Blue" | "Green" | "Orange"
    :
    T extends "MK.VII Liberty Combat Knife"
    ? "Default" | "Desert" | "Sea" | "Tundra"
    :
    T extends "Waveform"
    ? "Default" | "Black" | "Red" | "Purple/Pink"
    :
    T extends "Obsidiana"
    ? "Default"
    :
    T extends "Composite Knife"
    ? "Default"
    :
    T extends "Bolt Knife"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "Aperture Stiletto"
    ? "Default"
    :
    T extends "Overlay Dagger"
    ? "Default" | "Black" | "Pink" | "Grey"
    :
    T extends "Champions 2021 Karambit"
    ? "Default"
    :
    T extends "Champions 2022 Butterfly Knife"
    ? "Default"
    :
    T extends "Champions 2023 Kunai"
    ? "Default"
    :
    T extends "Champions 2024 Blade"
    ? "Default"
    :
    T extends "Origin Crescent Blade"
    ? "Default"
    :
    T extends "Striker Knife"
    ? "Default" | "Green/Yellow/Black" | "Pink/Teal/Purple" | "Blue/White/Red"
    :
    T extends "Comet Sword"
    ? "Default" | "Red" | "Pink" | "Yellow"
    :
    T extends "Radiant Crisis 001 Baseball Bat"
    ? "Default"
    :
    T extends "Nebula Knife"
    ? "Default"
    :
    T extends "Nocturnum Scythe"
    ? "Default" | "Black" | "Blue" | "Yellow"
    :
    T extends "Hack"
    ? "Default"
    :
    T extends "Doombringer Battleaxe"
    ? "Default" | "Red" | "Blue" | "Green"
    :
    T extends "Glitchpop Dagger"
    ? "Default"
    :
    T extends "Glitchpop Axe"
    ? "Default"
    :
    T extends "Terminus A Quo"
    ? "Default" | "Purple" | "Red" | "Black"
    :
    T extends "Emberclad Hammer"
    ? "Default"
    :
    T extends "Caeruleus"
    ? "Default"
    :
    T extends "Blade of Chaos"
    ? "Default" | "Green" | "White" | "Blue"
    :
    T extends "Elderflame Dagger"
    ? "Default"
    :
    T extends "Blades of Imperium"
    ? "Default" | "Ruby" | "Pearl" | "Obsidian"
    :
    T extends "Singularity Knife"
    ? "Default"
    :
    T extends "Singularity Butterfly Knife"
    ? "Default" | "Red" | "White" | "Blue"
    :
    T extends "Blade of Serket"
    ? "Default"
    :
    T extends "Songsteel"
    ? "Default"
    :
    T extends "Chromedek Gauntlet"
    ? "Default"
    :
    T extends "VCT LOCK//IN Misericórdia"
    ? "Default" | "Pacific" | "Americas" | "EMEA"
    :
    T extends "Hivemind Sword"
    ? "Default"
    :
    T extends "Venturi Knife"
    ? "Default"
    :
    T extends "Forsaken Ritual Blade"
    ? "Default"
    :
    T extends "Velocity Karambit"
    ? "Default" | "Yellow" | "Purple" | "Aquamarine"
    :
    T extends "Ruin Dagger"
    ? "Default"
    :
    T extends ".SYS Melee"
    ? "Default"
    :
    T extends "Neo Frontier Axe"
    ? "Default" | "Gold" | "Silver" | "Copper"
    :
    T extends "Soulstrife Scythe"
    ? "Default"
    :
    T extends "Blades of Primordia"
    ? "Default" | "Venom" | "Cobalt" | "Gold"
    :
    T extends "Hieroscape Blades"
    ? "Default"
    :
    T extends "EX.O Edge"
    ? "Default" | "Yellow" | "Red" | "Pink"
    :
    T extends "Prime Axe"
    ? "Default"
    :
    T extends "Prime//2.0 Karambit"
    ? "Default"
    :
    T extends "XERØFANG Knife"
    ? "Default" | "Black" | "Pink" | "Silver"
    :
    T extends "Bubble Pop Light Stick"
    ? "Default" | "Red"
    :
    T extends "Outpost Melee"
    ? "Default"
    :
    T extends "Guardrail Hammer"
    ? "Default"
    :
    T extends "Black.Market Butterfly Knife"
    ? "Default"
    :
    T extends "Bound "
    ? "Default"
    :
    T extends "Prism Knife"
    ? "Default"
    :
    T extends "Prism III Axe"
    ? "Default" | "Pink" | "Green" | "Purple"
    :
    T extends "Broken Blade of the Ruined King"
    ? "Default" | "Purple" | "Red/Yellow" | "Green"
    :
    T extends "Kingdom Knife"
    ? "Default"
    :
    T extends "K/TAC Blade"
    ? "Default"
    :
    T extends "Equilibrium"
    ? "Default" | "Dark"
    :
    T extends "Blade of Aemondir"
    ? "Default" | "Silver" | "Bronze" | "Obsidian"
    :
    T extends "Smite Knife"
    ? "Default"
    :
    T extends "Smite Hammer"
    ? "Default"
    :
    T extends "Daydreams Crowbar"
    ? "Default"
    :
    T extends "Catrina"
    ? "Default" | "Living" | "Eternal"
    :
    T extends "Iridian Thorn Blade"
    ? "Default"
    :
    T extends "Celestial Fan"
    ? "Default"
    :
    T extends "Luxe Knife"
    ? "Default"
    :
    T extends "Arcane Gauntlets"
    ? "Default"
    :
    T extends "Magepunk Electroblade"
    ? "Default" | "Green" | "Purple" | "Orange"
    :
    T extends "Magepunk Shock Gauntlet"
    ? "Default" | "Green" | "Pink" | "Orange"
    :
    T extends "Magepunk Sparkswitch"
    ? "Default" | "Green" | "Black/Red" | "Yellow"
    :
    T extends "Spline Dagger"
    ? "Default"
    :
    T extends "Minima Karambit"
    ? "Default"
    :
    T extends "Combat Crafts Axe"
    ? "Default"
    :
    T extends "Ruyi Staff"
    ? "Default" | "Sunset" | "Dusk" | "Night"
    :
    T extends "Overdrive Blade"
    ? "Default" | "Cobalt" | "Bubblegum" | "Onyx"
    :
    T extends "Crimsonbeast Hammer"
    ? "Default"
    :
    T extends "Genesis Arc"
    ? "Default"
    :
    T extends "Kuronami no Yaiba"
    ? "Default" | "Purple" | "White" | "Black"
    :
    T extends "Ion Energy Sword"
    ? "Default"
    :
    T extends "Ion Karambit"
    ? "Default" | "Green" | "Yellow" | "Blue"
    :
    T extends "Oni Claw"
    ? "Default"
    :
    T extends "Onimaru Kunitsuna"
    ? "Default" | "Kumo" | "Hana" | "Tsubame"
    :
    T extends "Switchback Ascender"
    ? "Default"
    :
    T extends "Cryostasis Impact Drill"
    ? "Default"
    :
    T extends "Artisan Foil"
    ? "Default" | "Red" | "Green" | "Blue"
    :
    T extends "Personal Administrative Melee Unit"
    ? "Default" | "White/Pink" | "Red/Blue" | "Green"
    :
    T extends "Luna's Descent"
    ? "Default"
    :
    T extends "Random Favorite Skin"
    ? "Default"
    :
    T extends "Gravitational Uranium Neuroblaster Baton"
    ? "Default"
    :
    T extends "Orion Sword"
    ? "Default"
    :
    T extends "Relic of the Sentinel"
    ? "Default" | "Pink" | "Red/Green" | "Blue/Purple"
    :
    T extends "Relic Stone Daggers"
    ? "Default" | "Pink" | "Red/Green" | "Blue/Purple"
    :
    T extends "Shellspire Sword"
    ? "Default"
    :
    T extends "Transition Knife"
    ? "Default" | "Blue" | "Orange" | "Green"
    :
    T extends "Byteshift Knife"
    ? "Default" | "Purple" | "Red" | "Yellow"
    :
    T extends "Torque Coiler"
    ? "Default" | "Yellow" | "Blue" | "Orange"
    :
    T extends "Helix Daggers"
    ? "Default" | "Gold" | "Purple" | "Silver"
    :
    T extends "Snowfall Wand"
    ? "Default"
    :
    T extends "Reaver Knife"
    ? "Default"
    :
    T extends "Reaver Karambit"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "Prosperity"
    ? "Default"
    :
    T extends "Sovereign Sword"
    ? "Default"
    :
    T extends "Eternal Sovereign"
    ? "Default" | "Green" | "Teal" | "Red"
    :
    T extends "Recon Balisong"
    ? "Default" | "RedCamo" | "BlueCamo" | "GreenCamo"
    :
    T extends "Titanmail Mace"
    ? "Default"
    :
    T extends "Mystbloom Kunai"
    ? "Default" | "Blue" | "Orange" | "Purple"
    :
    T extends "Reverie Sword"
    ? "Default"
    :
    T extends "Melee"
    ? "Default"
    :
    T extends "Evori's Spellcaster"
    ? "Default" | "Pink" | "Orange" | "Green"
    :
    T extends "Tilde Knife"
    ? "Default" | "Red" | "Purple" | "Green"
    :
    T extends "CYRAX Fanblade"
    ? "Default" | "Red" | "Yellow" | "Purple"
    :
    T extends "Fortune's Scepter "
    ? "Default"
    :
    T extends "Hu Else"
    ? "Default"
    :
    T extends "NO LIMITS Bat"
    ? "Default"
    :
    T extends "Sandswept Dagger"
    ? "Default" | "Ivory" | "Indigo" | "Scarlet"
    :
    T extends "BlastX Polymer KnifeTech Coated Knife"
    ? "Default" | "Black" | "Yellow" | "Pink"
    :
    T extends "Wonderstallion Hammer"
    ? "Default"
    :
    T extends "Ego Knife"
    ? "Default" | "Red" | "Tan" | "Pink"
    :
    T extends "VCT 2025 Karambit"
    ? "Default" | "CN" | "Pacific" | "Americas" | "EMEA"
    :
    T extends "Solarex Relic"
    ? "Default" | "Yellow" | "Green" | "Purple"
    :
    T extends "Kaimana"
    ? "Default" | "DeepSea"
    :
    T extends "Ignite Fan"
    ? "Default" | "Purple"
    :
    T extends "Winterwunderland Candy Cane"
    ? "Default" | "Green" | "Blue" | "Rainbow"
    :
    T extends "Storm Maw Axe"
    ? "Default"
    :
  unknown;
