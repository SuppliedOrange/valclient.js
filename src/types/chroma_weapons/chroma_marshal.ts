/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type MarshalChromaType<T> =   T extends "Task Force 809 Marshal"
    ? "Default"
    :
    T extends "Fiber Optic Marshal"
    ? "Default" | "Silver" | "Gold" | "Emerald"
    :
    T extends "Gaia's Vengeance Marshal"
    ? "Default" | "Blue" | "Green" | "Orange"
    :
    T extends "Composite Marshal"
    ? "Default"
    :
    T extends "Rune Stone Marshal"
    ? "Default"
    :
    T extends "Nocturnum Marshal"
    ? "Default" | "Black" | "Blue" | "Yellow"
    :
    T extends "Doom Wing Marshal"
    ? "Default"
    :
    T extends "Doodle Buds Marshal"
    ? "Default" | "Tactifriends" | "LeagueofLegends"
    :
    T extends "Songsteel Marshal"
    ? "Default"
    :
    T extends "Chromedek Marshal"
    ? "Default"
    :
    T extends "Venturi Marshal"
    ? "Default"
    :
    T extends "Ruin Marshal"
    ? "Default"
    :
    T extends "Neo Frontier Marshal"
    ? "Default" | "Gold" | "Silver" | "Copper"
    :
    T extends "Monarch Marshal"
    ? "Default"
    :
    T extends "Avalanche Marshal"
    ? "Default"
    :
    T extends "Black.Market Marshal"
    ? "Default"
    :
    T extends "Couture Marshal"
    ? "Default"
    :
    T extends "Magepunk Marshal"
    ? "Default" | "Green" | "Purple" | "Orange"
    :
    T extends "Tacti-Series Marshal"
    ? "Default"
    :
    T extends "Signature Marshal"
    ? "Default"
    :
    T extends "Coalition: Cobra Marshal"
    ? "Default" | "Blue" | "Red" | "Orange"
    :
    T extends "Minima Marshal"
    ? "Default"
    :
    T extends "Galleria Marshal"
    ? "Default"
    :
    T extends "Crimsonbeast Marshal"
    ? "Default"
    :
    T extends "Moondash Marshal"
    ? "Default"
    :
    T extends "Kuronami Marshal"
    ? "Default" | "Purple" | "White" | "Black"
    :
    T extends "Switchback Marshal"
    ? "Default"
    :
    T extends "POLYfrog Marshal"
    ? "Default"
    :
    T extends "Divine Swine Marshal"
    ? "Default"
    :
    T extends "Artisan Marshal"
    ? "Default" | "Red" | "Green" | "Blue"
    :
    T extends "Luna Marshal"
    ? "Default"
    :
    T extends "Premiere Collision Marshal"
    ? "Default"
    :
    T extends "Sovereign Marshal"
    ? "Default" | "Green" | "Silver" | "Purple"
    :
    T extends "Reverie Marshal"
    ? "Default"
    :
    T extends "Standard Marshal"
    ? "Default"
    :
    T extends "Freehand Marshal"
    ? "Default"
    :
    T extends "Spellbound Marshal"
    ? "Default"
    :
    T extends "Sandswept Marshal"
    ? "Default" | "Ivory" | "Indigo" | "Scarlet"
    :
    T extends "Wasteland Marshal"
    ? "Default"
    :
    T extends "Winterwunderland Marshal"
    ? "Default"
    :
    T extends "Convergence Marshal"
    ? "Default"
    :
  unknown;
