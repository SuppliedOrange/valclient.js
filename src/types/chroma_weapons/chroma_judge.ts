/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type JudgeChromaType<T> =   T extends "Divergence Judge"
    ? "Default" | "Purple" | "Blue" | "Green"
    :
    T extends "MK.VII Liberty Judge"
    ? "Default" | "Desert" | "Sea" | "Tundra"
    :
    T extends "Piedra del Sol Judge"
    ? "Default"
    :
    T extends "Bumble Brigade Judge"
    ? "Default"
    :
    T extends "Tacti-Treat Judge"
    ? "Default"
    :
    T extends "Undercity Judge"
    ? "Default"
    :
    T extends "Doombringer Judge"
    ? "Default" | "Red" | "Blue" | "Green"
    :
    T extends "Glitchpop Judge"
    ? "Default" | "Blue" | "Red" | "Gold"
    :
    T extends "ChronoVoid Judge"
    ? "Default" | "Purple" | "Red" | "Black"
    :
    T extends "Elderflame Judge"
    ? "Default" | "Red" | "Blue" | "Dark"
    :
    T extends "Imperium Judge"
    ? "Default" | "Ruby" | "Pearl" | "Obsidian"
    :
    T extends "Rush Judge"
    ? "Default"
    :
    T extends "Team Ace Judge"
    ? "Default"
    :
    T extends "Shimmer Judge"
    ? "Default" | "Gold" | "Red" | "Teal"
    :
    T extends "Nanobreak Judge"
    ? "Default"
    :
    T extends ".EXE Judge"
    ? "Default"
    :
    T extends "Digihex Judge"
    ? "Default" | "Red" | "Green" | "Gold"
    :
    T extends "Goldwing Judge"
    ? "Default"
    :
    T extends "Hydrodip Judge"
    ? "Default"
    :
    T extends "Bubble Pop Judge"
    ? "Default" | "Red"
    :
    T extends "Outpost Judge"
    ? "Default"
    :
    T extends "Bound Judge"
    ? "Default"
    :
    T extends "Prism III Judge"
    ? "Default" | "Pink" | "Green" | "Purple"
    :
    T extends "Serenity Judge"
    ? "Default"
    :
    T extends "Kohaku & Matsuba Judge"
    ? "Default" | "Dark"
    :
    T extends "Smite Judge"
    ? "Default"
    :
    T extends "Daydreams Judge"
    ? "Default"
    :
    T extends "Iridian Thorn Judge"
    ? "Default"
    :
    T extends "Celestial Judge"
    ? "Default"
    :
    T extends "Luxe Judge"
    ? "Default"
    :
    T extends "Coalition: Cobra Judge"
    ? "Default" | "Blue" | "Red" | "Orange"
    :
    T extends "Combat Crafts Judge"
    ? "Default"
    :
    T extends "Crimsonbeast Judge"
    ? "Default"
    :
    T extends "POLYfox Judge"
    ? "Default"
    :
    T extends "Divine Swine Judge"
    ? "Default"
    :
    T extends "Jigsaw Judge"
    ? "Default" | "Skye" | "Astra" | "Yoru"
    :
    T extends "Sensation Judge"
    ? "Default"
    :
    T extends "Silhouette Judge"
    ? "Default"
    :
    T extends "Byteshift Judge"
    ? "Default" | "Purple" | "Red" | "Yellow"
    :
    T extends "Snowfall Judge"
    ? "Default"
    :
    T extends "Sovereign Judge"
    ? "Default" | "Green" | "Teal" | "Red"
    :
    T extends "Mystbloom Judge"
    ? "Default" | "Blue" | "Orange" | "Purple"
    :
    T extends "Standard Judge"
    ? "Default"
    :
    T extends "Tilde Judge"
    ? "Default" | "Red" | "Purple" | "Green"
    :
    T extends "Gridcrash Judge"
    ? "Default"
    :
    T extends "Convex Judge"
    ? "Default"
    :
    T extends "Varnish Judge"
    ? "Default"
    :
    T extends "Storm Maw Judge"
    ? "Default"
    :
  unknown;
