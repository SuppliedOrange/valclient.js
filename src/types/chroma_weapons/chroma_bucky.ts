/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type BuckyChromaType<T> =   T extends "Altitude Bucky"
    ? "Default"
    :
    T extends "Xenohunter Bucky"
    ? "Default"
    :
    T extends "Frequency Bucky"
    ? "Default"
    :
    T extends "Gaia's Vengeance Bucky"
    ? "Default" | "Blue" | "Green" | "Orange"
    :
    T extends "Piedra del Sol Bucky"
    ? "Default"
    :
    T extends "Origin Bucky"
    ? "Default" | "Green" | "Red" | "White"
    :
    T extends "Radiant Crisis 001 Bucky"
    ? "Default"
    :
    T extends "Bulletbox Bucky"
    ? "Default"
    :
    T extends "Red Alert Bucky"
    ? "Default"
    :
    T extends "Surge Bucky"
    ? "Default" | "Black" | "Yellow" | "Blue"
    :
    T extends "Topotek Bucky"
    ? "Default"
    :
    T extends "Moon Scout Bucky"
    ? "Default"
    :
    T extends ".SYS Bucky"
    ? "Default"
    :
    T extends "Horizon Bucky"
    ? "Default"
    :
    T extends "Monarch Bucky"
    ? "Default"
    :
    T extends "Hydrodip Bucky"
    ? "Default"
    :
    T extends "Prime//2.0 Bucky"
    ? "Default" | "Gold" | "Green" | "Orange"
    :
    T extends "Prism II Bucky"
    ? "Default"
    :
    T extends "Retrowave Bucky"
    ? "Default" | "Navy" | "Gold" | "Violet"
    :
    T extends "Kingdom Bucky"
    ? "Default"
    :
    T extends "Aemondir Bucky"
    ? "Default" | "Silver" | "Bronze" | "Obsidian"
    :
    T extends "Smite Bucky"
    ? "Default"
    :
    T extends "Iridian Thorn Bucky"
    ? "Default"
    :
    T extends "Magepunk Bucky"
    ? "Default" | "Green" | "Purple" | "Orange"
    :
    T extends "Overdrive Bucky"
    ? "Default" | "Cobalt" | "Bubblegum" | "Onyx"
    :
    T extends "Galleria Bucky"
    ? "Default"
    :
    T extends "Genesis Bucky"
    ? "Default"
    :
    T extends "Ion Bucky"
    ? "Default"
    :
    T extends "Oni Bucky"
    ? "Default" | "Kumo" | "Hana" | "Tsubame"
    :
    T extends "Artisan Bucky"
    ? "Default" | "Red" | "Green" | "Blue"
    :
    T extends "Lightwave Bucky"
    ? "Default" | "Blue" | "Red" | "Gray"
    :
    T extends "Gravitational Uranium Neuroblaster Bucky"
    ? "Default" | "Chrome" | "Black" | "Red/Blue"
    :
    T extends "Panoramic Bucky"
    ? "Default"
    :
    T extends "Cavalier Bucky"
    ? "Default"
    :
    T extends "Titanmail Bucky"
    ? "Default"
    :
    T extends "Standard Bucky"
    ? "Default"
    :
    T extends "Spellbound Bucky"
    ? "Default"
    :
    T extends "Aerosol Bucky"
    ? "Default"
    :
  unknown;
