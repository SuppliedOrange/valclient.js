/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type SheriffChromaType<T> =   T extends "Wayfarer Sheriff"
    ? "Default"
    :
    T extends "Mythmaker Sheriff"
    ? "Default"
    :
    T extends "Immortalized Sheriff"
    ? "Default" | "Orange" | "Blue" | "Green"
    :
    T extends "RGX 11z Pro Sheriff"
    ? "Default" | "Red" | "Blue" | "Yellow"
    :
    T extends "Altitude Sheriff"
    ? "Default"
    :
    T extends "Araxys Sheriff"
    ? "Default" | "Purple" | "Black" | "Silver"
    :
    T extends "Aristocrat Sheriff"
    ? "Default"
    :
    T extends "Composite Sheriff"
    ? "Default"
    :
    T extends "Overlay Sheriff"
    ? "Default" | "Black" | "Pink" | "Grey"
    :
    T extends "Nebula Sheriff"
    ? "Default"
    :
    T extends "Doombringer Sheriff"
    ? "Default" | "Red" | "Blue" | "Green"
    :
    T extends "ChronoVoid Sheriff"
    ? "Default" | "Purple" | "Red" | "Black"
    :
    T extends "Abyssal Sheriff"
    ? "Default"
    :
    T extends "Imperium Sheriff"
    ? "Default" | "Ruby" | "Pearl" | "Obsidian"
    :
    T extends "Singularity Sheriff"
    ? "Default" | "Blue" | "Red" | "Purple"
    :
    T extends "Surge Sheriff"
    ? "Default" | "Black" | "Yellow" | "Blue"
    :
    T extends "Moon Scout Sheriff"
    ? "Default"
    :
    T extends "Nanobreak Sheriff"
    ? "Default"
    :
    T extends ".SYS Sheriff"
    ? "Default"
    :
    T extends "Neo Frontier Sheriff"
    ? "Default" | "Gold" | "Silver" | "Copper"
    :
    T extends "EX.O Sheriff"
    ? "Default" | "Yellow" | "Red" | "Pink"
    :
    T extends "Protektor Sheriff"
    ? "Default"
    :
    T extends "Prism II Sheriff"
    ? "Default"
    :
    T extends "Retrowave Sheriff"
    ? "Default" | "Navy" | "Gold" | "Violet"
    :
    T extends "K/TAC Sheriff"
    ? "Default"
    :
    T extends "Aemondir Sheriff"
    ? "Default" | "Silver" | "Bronze" | "Obsidian"
    :
    T extends "Monstrocity Sheriff"
    ? "Default" | "Brown" | "Blue" | "Yellow"
    :
    T extends "Iridian Thorn Sheriff"
    ? "Default"
    :
    T extends "Magepunk Sheriff"
    ? "Default" | "Green" | "Pink" | "Orange"
    :
    T extends "Signature Sheriff"
    ? "Default"
    :
    T extends "Minima Sheriff"
    ? "Default"
    :
    T extends "Overdrive Sheriff"
    ? "Default" | "Cobalt" | "Bubblegum" | "Onyx"
    :
    T extends "Silvanus Sheriff"
    ? "Default"
    :
    T extends "Crimsonbeast Sheriff"
    ? "Default"
    :
    T extends "Kuronami Sheriff"
    ? "Default" | "Purple" | "White" | "Black"
    :
    T extends "Ion Sheriff"
    ? "Default"
    :
    T extends "POLYfox Sheriff"
    ? "Default"
    :
    T extends "POLYfrog Sheriff"
    ? "Default"
    :
    T extends "Protocol 781-A Sheriff"
    ? "Default" | "White/Pink" | "Red/Blue" | "Green"
    :
    T extends "Lightwave Sheriff"
    ? "Default" | "Blue" | "Red" | "Gray"
    :
    T extends "Sentinels of Light Sheriff"
    ? "Default" | "Pink" | "Red/Green" | "Blue/Purple"
    :
    T extends "Sakura Sheriff"
    ? "Default"
    :
    T extends "Peacekeeper Sheriff"
    ? "Default"
    :
    T extends "Schema Sheriff"
    ? "Default"
    :
    T extends "Reaver Sheriff"
    ? "Default" | "Red" | "Black" | "White"
    :
    T extends "Mystbloom Sheriff"
    ? "Default" | "Blue" | "Orange" | "Purple"
    :
    T extends "Standard Sheriff"
    ? "Default"
    :
    T extends "Cloudweaver Sheriff"
    ? "Default"
    :
    T extends "Sandswept Sheriff"
    ? "Default" | "Ivory" | "Indigo" | "Scarlet"
    :
    T extends "Convex Sheriff"
    ? "Default"
    :
    T extends "Solarex Sheriff"
    ? "Default" | "Yellow" | "Green" | "Purple"
    :
    T extends "Varnish Sheriff"
    ? "Default"
    :
    T extends "Wasteland Sheriff"
    ? "Default"
    :
    T extends "Game Over Sheriff"
    ? "Default"
    :
    T extends "Arcane Sheriff"
    ? "Default"
    :
    T extends "Death Wish Sheriff"
    ? "Default"
    :
  unknown;
