/* eslint-disable prettier/prettier */
/* FILE GENERATED AUTOMATICALLY */

/* This file is split to avoid ESLint stack overflow */

export type ShortyChromaType<T> =   T extends "Hard Bargain Shorty"
    ? "Default"
    :
    T extends "Sidekick Shorty"
    ? "Default"
    :
    T extends "Araxys Shorty"
    ? "Default" | "Purple" | "Black" | "Silver"
    :
    T extends "Neptune Shorty"
    ? "Default" | "Black"
    :
    T extends "Gaia's Vengeance Shorty"
    ? "Default" | "Blue" | "Green" | "Orange"
    :
    T extends "Karabasan Shorty"
    ? "Default"
    :
    T extends "Rune Stone Shorty"
    ? "Default"
    :
    T extends "Doom Wing Shorty"
    ? "Default"
    :
    T extends "Prelude to Chaos Shorty"
    ? "Default" | "Green" | "White" | "Blue"
    :
    T extends "Doodle Buds Shorty"
    ? "Default" | "Tactifriends" | "LeagueofLegends"
    :
    T extends "Chromedek Shorty"
    ? "Default"
    :
    T extends "Hivemind Shorty"
    ? "Default"
    :
    T extends "Velocity Shorty"
    ? "Default" | "Yellow" | "Purple" | "Aquamarine"
    :
    T extends "Ruin Shorty"
    ? "Default"
    :
    T extends "Primordium Shorty"
    ? "Default" | "Venom" | "Cobalt" | "Gold"
    :
    T extends "Monarch Shorty"
    ? "Default"
    :
    T extends "Guardrail Shorty"
    ? "Default"
    :
    T extends "Prism II Shorty"
    ? "Default"
    :
    T extends "Aquatica Shorty"
    ? "Default"
    :
    T extends "Wunderkind Shorty"
    ? "Default"
    :
    T extends "Genesis Shorty"
    ? "Default"
    :
    T extends "Oni Shorty"
    ? "Default" | "Kumo" | "Hana" | "Tsubame"
    :
    T extends "Snakebite Shorty"
    ? "Default"
    :
    T extends "Hue Shift Shorty"
    ? "Default"
    :
    T extends "Sentinels of Light Shorty"
    ? "Default" | "Pink" | "Red/Green" | "Blue/Purple"
    :
    T extends "Transition Shorty"
    ? "Default" | "Blue" | "Orange" | "Green"
    :
    T extends "Standard Shorty"
    ? "Default"
    :
    T extends "Aerosol Shorty"
    ? "Default"
    :
    T extends "Tilde Shorty"
    ? "Default" | "Red" | "Purple" | "Green"
    :
    T extends "Cloudweaver Shorty"
    ? "Default"
    :
    T extends "Tigris Shorty"
    ? "Default"
    :
    T extends "Wayfinder Shorty"
    ? "Default"
    :
    T extends "Wasteland Shorty"
    ? "Default"
    :
  unknown;
